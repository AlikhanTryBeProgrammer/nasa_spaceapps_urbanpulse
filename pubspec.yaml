name: nasa_growth_app
description: A Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.63.0
  cupertino_icons: ^1.0.8
  http: ^1.5.0
  provider: ^6.0.5
  characters: ^1.4.0
  latlong2: ^0.9.1
  webview_flutter: ^4.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/
